import { Box, Grid } from "@mui/material";
import React from "react";

const ProductsList = () => {
  return (
    <Grid item md={9}>
      <Box sx={{ display: "flex" }}>products</Box>
    </Grid>
  );
};

export default ProductsList;
